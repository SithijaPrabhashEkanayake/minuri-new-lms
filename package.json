{
  "name": "minuri-new-lms",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix minuri-lms",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix minuri-lms\" \"node backend/server.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
